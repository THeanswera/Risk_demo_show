"""
Расчет tр и tск по Приложению 6 Методики N 1140
(упрощенная аналитическая модель движения людского потока).

Параметры движения по Приложению 2 (Таблицы П2.6, П2.7, П2.8).
Формулы скорости и интенсивности: П2.1-П2.3.
Алгоритм расчета: формулы П6.1-П6.12.
"""

import math
from utils.helpers import safe_float


# ---------------------------------------------------------------------------
# Таблица П2.6 - параметры движения людского потока, группы М0
# Формат: FLOW_PARAMS_M0[группа][вид_пути] = (V0, a, D0)
#   V0 - средняя скорость свободного движения, м/мин
#   a  - коэффициент адаптации
#   D0 - предельная плотность свободного движения, м2/м2
# ---------------------------------------------------------------------------

FLOW_PARAMS_M0: dict[str, dict[str, tuple[float, float, float]]] = {
    "М0-1": {  # Дети и подростки 7-18 лет, f=0.06
        "горизонтальный": (92.6, 0.284, 0.75),
        "проем":          (92.6, 0.350, 1.20),
        "лестница_вниз":  (92.4, 0.338, 0.94),
        "лестница_вверх": (65.9, 0.289, 0.84),
    },
    "М0-2": {  # Молодежь 18-25 лет, f=0.09
        "горизонтальный": (120.0, 0.308, 0.72),
        "проем":          (120.0, 0.308, 0.53),
        "лестница_вниз":  (129.0, 0.353, 0.58),
        "лестница_вверх": (76.8, 0.305, 0.67),
    },
    "М0-3": {  # Трудоспособный возраст 18-60 лет, f=0.10
        "горизонтальный": (100.0, 0.295, 0.51),
        "проем":          (100.0, 0.295, 0.65),
        "лестница_вниз":  (100.0, 0.400, 0.89),
        "лестница_вверх": (60.0, 0.305, 0.67),
    },
    "М0-4": {  # Дети + трудоспособные, f=0.09
        "горизонтальный": (93.8, 0.353, 0.56),
        "проем":          (93.8, 0.371, 0.64),
        "лестница_вниз":  (93.8, 0.394, 0.75),
        "лестница_вверх": (57.5, 0.375, 0.66),
    },
    "М0-5": {  # Дети + трудоспособные + пожилые активные, f=0.121
        "горизонтальный": (91.4, 0.357, 0.58),
        "проем":          (91.8, 0.366, 0.62),
        "лестница_вниз":  (90.0, 0.410, 0.83),
        "лестница_вверх": (56.1, 0.379, 0.68),
    },
    "М0-6": {  # Трудоспособные + пожилые активные, f=0.127
        "горизонтальный": (69.6, 0.385, 0.71),
        "проем":          (72.1, 0.318, 0.41),
        "лестница_вниз":  (61.7, 0.394, 0.75),
        "лестница_вверх": (43.5, 0.400, 0.78),
    },
    "М0-7": {  # С грудными детьми + все остальные, f=0.121
        "горизонтальный": (45.02, 0.425, 0.86),
        "проем":          (50.0, 0.253, 0.18),
        "лестница_вниз":  (30.0, 0.367, 0.62),
        "лестница_вверх": (30.0, 0.414, 0.88),
    },
}


# ---------------------------------------------------------------------------
# Таблица П2.7 - параметры движения МГН
# ---------------------------------------------------------------------------

FLOW_PARAMS_MGN: dict[str, dict[str, tuple[float, float, float]]] = {
    "Пожилые (старше 60)": {
        "горизонтальный": (80.0, 0.295, 0.51),
        "проем":          (80.0, 0.295, 0.65),
        "лестница_вниз":  (70.0, 0.400, 0.89),
        "лестница_вверх": (60.0, 0.305, 0.67),
    },
    "Пожилые немощные (М2)": {
        "горизонтальный": (25.0, 0.428, 0.96),
        "проем":          (20.0, 0.456, 1.02),
        "лестница_вниз":  (20.0, 0.505, 1.26),
        "лестница_вверх": (20.0, 0.338, 0.56),
    },
    "Дошкольники": {
        "горизонтальный": (60.0, 0.275, 0.78),
        "проем":          (60.0, 0.350, 1.20),
        "лестница_вниз":  (47.0, 0.190, 0.64),
        "лестница_вверх": (47.0, 0.275, 0.76),
    },
    "Дети с ОВЗ": {
        "горизонтальный": (51.0, 0.29, 0.60),
        "проем":          (47.0, 0.30, 0.67),
        "лестница_вниз":  (23.0, 0.21, 0.63),
        "лестница_вверх": (20.0, 0.30, 0.69),
    },
    "С поражением ОДА (М3)": {
        "горизонтальный": (44.0, 0.414, 0.77),
        "проем":          (38.0, 0.345, 0.57),
        "лестница_вниз":  (24.0, 0.422, 0.96),
        "лестница_вверх": (14.0, 0.313, 0.74),
    },
    "Слепые/слабовидящие (М2)": {
        "горизонтальный": (26.0, 0.371, 0.73),
        "проем":          (17.0, 0.271, 0.77),
        "лестница_вниз":  (21.0, 0.519, 0.97),
        "лестница_вверх": (18.0, 0.387, 0.82),
    },
    "Глухие/слабослышащие (М1)": {
        "горизонтальный": (82.0, 0.301, 0.58),
        "проем":          (82.0, 0.328, 0.73),
        "лестница_вниз":  (82.0, 0.380, 0.91),
        "лестница_вверх": (54.0, 0.344, 0.72),
    },
    "Беременные": {
        "горизонтальный": (56.42, 0.404, 0.991),
        "проем":          (49.47, 0.427, 1.033),
        "лестница_вниз":  (42.35, 0.336, 0.786),
        "лестница_вверх": (31.25, 0.411, 1.312),
    },
    "Инвалиды на колясках (М4)": {
        "горизонтальный": (60.0, 0.400, 0.141),
        "пандус_вниз":    (60.0, 0.400, 0.141),
        "пандус_вверх":   (40.0, 0.420, 0.156),
    },
}


# ---------------------------------------------------------------------------
# Площади горизонтальной проекции f, м2/чел (Приложение 2, п. 10)
# ---------------------------------------------------------------------------

F_PROJECTION: dict[str, float] = {
    "дошкольники": 0.03,
    "школьники": 0.06,
    "М0 лето": 0.10,
    "М0 зима": 0.125,
    "М1 глухие/слабослышащие": 0.125,
    "М2 пожилые немощные": 0.20,
    "М3 с одной опорой": 0.20,
    "М3 с двумя опорами": 0.30,
    "М2 слепые с тростью": 0.40,
    "М4 кресло-коляска": 0.96,
    "беременные": 0.15,
    "дети с ОВЗ": 0.15,
}


# ---------------------------------------------------------------------------
# Таблица П2.8 - максимальные интенсивности qmax (м/мин)
# ---------------------------------------------------------------------------

Q_MAX: dict[str, float] = {
    "горизонтальный": 16.5,
    "проем":          19.6,
    "лестница_вниз":  16.0,
    "лестница_вверх": 11.0,
    "пандус_вниз":    16.0,
    "пандус_вверх":   11.0,
}


# ---------------------------------------------------------------------------
# Объединенный словарь всех групп (для UI)
# ---------------------------------------------------------------------------

def get_all_flow_groups() -> dict[str, dict[str, tuple[float, float, float]]]:
    """Все группы контингента (М0 + МГН) для выбора в UI."""
    result = {}
    result.update(FLOW_PARAMS_M0)
    result.update(FLOW_PARAMS_MGN)
    return result


# ---------------------------------------------------------------------------
# Формула П2.1 - скорость движения при заданной плотности D
# ---------------------------------------------------------------------------

def velocity(D: float, path_type: str, flow_group: str) -> float:
    """
    Скорость V(D) по формуле П2.1 Приложения 2.

    D - плотность потока, м2/м2
    path_type - вид пути
    flow_group - группа контингента

    Возвращает V в м/мин.
    """
    all_groups = get_all_flow_groups()
    if flow_group not in all_groups:
        raise ValueError(f"Группа '{flow_group}' не найдена")
    params = all_groups[flow_group]
    if path_type not in params:
        raise ValueError(
            f"Вид пути '{path_type}' не найден для группы '{flow_group}'"
        )

    V0, a, D0 = params[path_type]

    if D <= 0:
        return V0

    # Формула П2.1
    if D <= D0:
        V = V0
    else:
        V = V0 * (1.0 - a * math.log(D / D0))
        V = max(V, 0.0)

    # Коэффициент m для проемов (после формулы П2.1)
    if path_type == "проем":
        if D >= 0.5:
            m = 1.25 - 0.05 * D
            m = max(m, 0.0)
        else:
            m = 1.0
        V = V * m

    return V


# ---------------------------------------------------------------------------
# Формула П2.2 - интенсивность q = V * D
# ---------------------------------------------------------------------------

def intensity(D: float, path_type: str, flow_group: str) -> float:
    """q(D) = V(D) * D - формула П2.2."""
    V = velocity(D, path_type, flow_group)
    return V * D


# ---------------------------------------------------------------------------
# Dmax - плотность при qmax (после формулы П2.3)
# Dmax = D0 * exp(1 - 1/a)
# ---------------------------------------------------------------------------

def density_at_qmax(path_type: str, flow_group: str) -> float:
    """Dmax = D0 * exp(1 - 1/a) - плотность при максимальной интенсивности."""
    all_groups = get_all_flow_groups()
    params = all_groups[flow_group]
    _, a, D0 = params[path_type]
    if a <= 0:
        return D0
    return D0 * math.exp(1.0 - 1.0 / a)


# ---------------------------------------------------------------------------
# Обратная задача: найти D по заданной q и виду пути
# Используем численный поиск (бисекция), т.к. q(D) немонотонна
# ---------------------------------------------------------------------------

def _find_density_for_intensity(
    q_target: float, path_type: str, flow_group: str
) -> float:
    """
    Найти плотность D при заданной интенсивности q.
    Ищем решение на ветви D <= Dmax (до максимума интенсивности).
    """
    D_max = density_at_qmax(path_type, flow_group)

    # Бисекция на [0, D_max]
    lo, hi = 1e-6, D_max
    for _ in range(100):
        mid = (lo + hi) / 2.0
        q_mid = intensity(mid, path_type, flow_group)
        if q_mid < q_target:
            lo = mid
        else:
            hi = mid
    return (lo + hi) / 2.0


# ---------------------------------------------------------------------------
# Основная функция: расчет tр и tск по Приложению 6
# Формулы П6.1-П6.12
# ---------------------------------------------------------------------------

def calc_evacuation(
    segments: list[dict],
    flow_group: str = "М0-3",
    f_projection: float = 0.10,
) -> dict:
    """
    Расчет tр и tск по Приложению 6 Методики N 1140.

    Параметры
    ---------
    segments : список участков маршрута эвакуации
        Каждый элемент - dict с ключами:
            "тип": str - "горизонтальный", "проем", "лестница_вниз", "лестница_вверх"
            "длина": float - длина участка, м (для проемов = 0)
            "ширина": float - ширина участка, м
            "людей": int - количество людей (только для первого участка)
    flow_group : группа контингента из Таблицы П2.6 или П2.7
    f_projection : площадь горизонтальной проекции, м2/чел

    Возвращает
    ----------
    dict:
        "t_p": float - расчетное время эвакуации, мин
        "t_ck": float - время существования скоплений, мин (0 если нет)
        "segments_detail": list[dict] - детализация по каждому участку
        "congestion_segments": list[int] - номера участков со скоплением
        "warnings": list[str] - предупреждения
    """
    if not segments:
        return {
            "t_p": 0.0,
            "t_ck": 0.0,
            "segments_detail": [],
            "congestion_segments": [],
            "warnings": ["Пустой список участков"],
        }

    f = safe_float(f_projection, 0.10)
    if f <= 0:
        f = 0.10

    warnings: list[str] = []
    details: list[dict] = []
    congestion_ids: list[int] = []
    total_t = 0.0
    total_tz = 0.0
    total_tck = 0.0

    # Шаг 1: начальный участок (П6.2)
    seg0 = segments[0]
    path_type_0 = seg0.get("тип", "горизонтальный")
    l_0 = safe_float(seg0.get("длина", 0), 0)
    b_0 = safe_float(seg0.get("ширина", 1), 1)
    N_0 = int(safe_float(seg0.get("людей", 0), 0))

    if b_0 <= 0:
        b_0 = 1.0
    if l_0 <= 0 and path_type_0 != "проем":
        l_0 = 0.01

    # Плотность на начальном участке (П6.2): D1 = N * f / (l * b)
    if l_0 > 0 and b_0 > 0:
        D_0 = N_0 * f / (l_0 * b_0)
    else:
        D_0 = 0.0

    V_0 = velocity(D_0, path_type_0, flow_group)
    q_0 = D_0 * V_0  # интенсивность (П2.2)

    if l_0 > 0 and V_0 > 0:
        t_0 = l_0 / V_0
    else:
        t_0 = 0.0

    total_t += t_0

    details.append({
        "N": 1,
        "тип": path_type_0,
        "длина": round(l_0, 3),
        "ширина": round(b_0, 3),
        "D": round(D_0, 3),
        "V": round(V_0, 3),
        "q": round(q_0, 3),
        "t": round(t_0, 3),
        "скопление": False,
        "tз": 0.0,
    })

    prev_q = q_0
    prev_b = b_0
    # Общее число людей в потоке (для расчета задержек)
    N_total = N_0

    # Шаг 2: последующие участки (П6.4)
    for i_seg in range(1, len(segments)):
        seg = segments[i_seg]
        path_type = seg.get("тип", "горизонтальный")
        l_i = safe_float(seg.get("длина", 0), 0)
        b_i = safe_float(seg.get("ширина", 1), 1)

        if b_i <= 0:
            b_i = 1.0

        # Интенсивность на границе участков (П6.4): q_i = q_(i-1) * b_(i-1) / b_i
        if b_i > 0:
            q_i = prev_q * prev_b / b_i
        else:
            q_i = prev_q

        # qmax для данного вида пути (Таблица П2.8)
        qmax = Q_MAX.get(path_type, 16.5)

        is_congestion = False
        tz = 0.0

        # Шаг 3: проверка скопления (П6.7)
        if q_i > qmax:
            is_congestion = True
            congestion_ids.append(i_seg + 1)

            # Движение при максимальной интенсивности
            D_max_i = density_at_qmax(path_type, flow_group)
            V_i = velocity(D_max_i, path_type, flow_group)
            D_i = D_max_i
            q_actual = qmax

            # Время задержки tз (П6.10-П6.12)
            # tз = N * f / (qmax * b_i) - N * f / (q_i * prev_b)
            # Упрощенно: задержка из-за превышения пропускной способности
            if qmax * b_i > 0:
                t_pass_through = N_total * f / (qmax * b_i)
            else:
                t_pass_through = 0.0
            if q_i * prev_b > 0:
                t_arrival = N_total * f / (q_i * prev_b)
            else:
                t_arrival = 0.0

            tz = max(0.0, t_pass_through - t_arrival)
            total_tz += tz

            warnings.append(
                f"Участок {i_seg + 1} ({path_type}): "
                f"q = {q_i:.2f} > qmax = {qmax:.1f} м/мин. "
                f"tз = {tz:.3f} мин."
            )
        else:
            # Определяем D по q (обратная задача)
            if q_i > 0:
                D_i = _find_density_for_intensity(q_i, path_type, flow_group)
            else:
                D_i = 0.0

            V_i = velocity(D_i, path_type, flow_group)
            q_actual = q_i

        # Время прохождения участка
        if l_i > 0 and V_i > 0:
            t_i = l_i / V_i
        else:
            t_i = 0.0

        total_t += t_i

        details.append({
            "N": i_seg + 1,
            "тип": path_type,
            "длина": round(l_i, 3),
            "ширина": round(b_i, 3),
            "D": round(D_i, 3),
            "V": round(V_i, 3),
            "q": round(q_actual, 3),
            "t": round(t_i, 3),
            "скопление": is_congestion,
            "tз": round(tz, 3),
        })

        prev_q = q_actual
        prev_b = b_i

    # Шаг 4: итог (П6.1)
    t_p = total_t + total_tz

    # tск - время существования скоплений
    # Скопление существует, пока все люди не пройдут узкое место
    # tск >= tз (всегда)
    t_ck = total_tz

    return {
        "t_p": round(t_p, 3),
        "t_ck": round(t_ck, 3),
        "segments_detail": details,
        "congestion_segments": congestion_ids,
        "warnings": warnings,
    }
